import React from 'react';
import './app.scss';

const App = () => (
    <div className="app">
        <header>
            <h1>ValueCentric</h1>
            <h2>create-react-app</h2>
        </header>
        <p>To get started, edit <code>src/App.js</code> then save and reload.</p>
    </div>
);

export default App;